Example SSH configuration in demo-tools. Transmitted to SSH configuration using `ssh-compile-config`.

The `config` file must at least exist, even if it is empty, as `ssh-compile-config` will only search for it.
